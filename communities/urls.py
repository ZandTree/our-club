from django.urls import path

#
# from . import views
#
# urlpatterns = [
#     path("", views.AllCommunities.as_view(), name="list"),
#     path("new/", views.CreateCommunity.as_view(), name="create"),
#     path("posts/in/<slug:slug>/",views.SingleCommunity.as_view(),name="single"),
#     path("join/<slug:slug>/",views.JoinCommunity.as_view(),name="join"),
#     path("leave/(<slug:slug>)/",views.LeaveCommunity.as_view(),name="leave"),
# ]
